<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>My task</title>
</head>
<body>
<div class="row">

  <div class="large-6 columns">
  <h2>Tasks created by me</h2>
      <table>
        <thead>
          <tr>
            <th>Title</th>
            <th>Duration</th>
            <th>Start date</th>
            <th>Finish date</th>
            <th>User</th>
            <th>Finished</th>
            <th colspan="3"></th>
          </tr>
        </thead>
      
        <tbody>
          <% @tasks.each do |task| %>
          <% if task.user_id == current_user.id %>
            <tr>
              <td><%= task.title %></td>
              <td><%= task.duration %></td>
              <td><%= task.start_date %></td>
              <td><%= task.finish_date %></td>
              <td><%= task.user.fullname %></td>
              <td><%= task.finished %></td>
              <td><%= link_to 'Show', task %></td>
              <td><%= link_to 'Edit', edit_task_path(task) %></td>
              <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
            <% end %>
          <% end %>
        </tbody>
      </table>
  </div>

  <div class="large-6 columns">
    <h2>Tasks I'm responsible of</h2>
    <fieldset>
      <table>
        <thead>
          <tr>
            <th>Title</th>
            <th>Duration</th>
            <th>Start date</th>
            <th>Finish date</th>
            <th>User</th>
            <th>Finished</th>
            <th colspan="3"></th>
          </tr>
        </thead>
      
        <tbody>
          <% @tasks.each do |task| %>
          <% if task.responsible_id == current_user.id %>
            <tr>
              <td><%= task.title %></td>
              <td><%= task.duration %></td>
              <td><%= task.start_date %></td>
              <td><%= task.finish_date %></td>
              <td><%= task.user.fullname %></td>
              <td><%= task.finished %></td>
              <td><%= link_to 'Show', task %></td>
              <td><%= link_to 'Edit', edit_task_path(task) %></td>
              <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
            <% end %>
          <% end %>
        </tbody>
      </table>
    </fieldset>
  </div>

</div>
	
</body>
</html>